[tool.poetry]
name = "module08-loading"
version = "0.1.0"
description = "Matrix loading example"
authors = ["student <student@example.com>"]

[tool.poetry.dependencies]
python = ">=3.10"
pandas = "^2.0"
numpy = "^1.25"
matplotlib = "^3.7"
requests = "^2.28"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
